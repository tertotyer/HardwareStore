@model IEnumerable<Thing>

@if (Model.Count() != 0)
{
    int totalCost = 0;
    @foreach (var thing in Model)
    {
        <p>@thing.Name @thing.Price</p>
        totalCost += thing.Price;
    }
    <h1>@totalCost</h1>
    <a asp-controller="Orders" asp-action="Create">Оформить заказ</a>
}
